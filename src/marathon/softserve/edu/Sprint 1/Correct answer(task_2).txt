//2 task


public class Employee {
	public String fullName;
	public float salary;

	public static void main(String[] args) {

		Employee emp1 = new Employee();
		emp1.fullName = "Anna";
		emp1.salary = 100000f;

		Employee emp2 = new Employee();
		emp2.fullName = "Kate";
		emp2.salary = 1000f;

		Employee[] employees = { emp1, emp2 };

		
		// my version
		/*-
                String employeesInfo = "";
   
		employeesInfo += "[";
		for (Employee employee : employees) {
			employeesInfo += String.format("{fullName: \"%s\", salary: %.1f}, ", employee.fullName, employee.salary);
		}
		employeesInfo = employeesInfo.substring(0, employeesInfo.length() - 2);
		employeesInfo += "]";
		*/

		// I correct version
		/*-
                String employeesInfo = "";
		for(int i=0; i<employees.length; i++) {
			employeesInfo = employeesInfo + "{fullName: \"" + employees[i].fullName + "\", salary: " + employees[i].salary;
			if(i == employees.length -1)
				employeesInfo = employeesInfo + "}]";
			else
				employeesInfo = employeesInfo + "}, ";
		}
		*/

		// II correct version
                /*
                String employeesInfo = ""; 
		for (Employee employee : employees) {
			employeesInfo += "{fullName: \"" + employee.fullName + "\", " + "salary: " + employee.salary + "}, ";
		}
		employeesInfo = employeesInfo.substring(0, employeesInfo.length() - 2);
		employeesInfo = "[" + employeesInfo + "]";
                */

                // III more correct version

                StringBuilder sb = new StringBuilder("[");
		for(int i=0; i<employees.length; i++) {
			sb.append("{fullName: \"")
			.append(employees[i].fullName)
			.append("\", salary: ")
			.append(employees[i].salary)
			.append("}, ");
		}
		sb.delete(sb.length()-2, sb.length());
		sb.append("]");
                String employeesInfo = sb.toString();

	}
}
